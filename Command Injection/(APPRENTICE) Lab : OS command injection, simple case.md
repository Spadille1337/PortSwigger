Intercept the request using `BurpSuite` and then `Modify` the storeID parameter, changing it to:

```
1 | whoami
```

` | ` is pipe operator.

Eg. `A|B` A and B are executed in parallel, and the standard output of A is sent as the standard input of B.
